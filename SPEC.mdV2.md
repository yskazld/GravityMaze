# SPEC.md V2
# 重力迷宮パズル（仮称） Unity実装仕様書

## 0. 本仕様書の目的
本仕様書は、UnityエンジニアまたはCodexに渡して、ゲームの実装方針を共有するための仕様書である。

Prefab、画像素材、ボタン配置、Hierarchy構成は開発者がUnity上で作成する。
ただし、本仕様書では実装に必要なクラス設計、ゲームルール、ステージ生成条件、データ構造、広告・ランキング方針を定義する。

---

## 1. ゲーム概要

### 1.1 ジャンル
重力変更型・迷宮パズルゲーム。

### 1.2 基本コンセプト
プレイヤーは上下左右の重力方向を切り替え、ステージ内を滑るように移動しながらGoalを目指す。

1回の入力で、Playerは指定方向へ壁・箱・障害物に当たるまで移動する。

### 1.3 ゲームの特徴
- 操作は上下左右の重力変更のみ
- PlayerとBoxが同じルールで滑る
- Boxを使って行き止まりを塞ぎ、道を作る
- Enemyは重力では動かず、個別の移動ルールで動く
- ランダム生成ステージを遊べる
- 累積星数やデイリー成績でランキングを競う

---

## 2. 基本ゲームルール

## 2.1 入力
プレイヤーは以下の4方向を選択できる。

- 上
- 下
- 左
- 右

入力された方向が、そのターンの重力方向となる。

---

## 2.2 Player移動仕様

### 移動ルール
Playerは入力方向に対して、以下のいずれかに当たるまで直線移動する。

- 壁
- ステージ外枠
- Box
- その他の障害物

### 移動方式
1マスずつ移動するのではなく、停止条件に当たるまで滑る。

例：

```text
P . .
. . .
. . G

↓入力

. . .
. . .
P . G
```

---

## 2.3 Box移動仕様

### 基本ルール
BoxもPlayerと同じく、入力方向へ滑る。

### 役割
Boxは単なる障害物ではなく、ステージ攻略に利用する重要オブジェクトである。

主な役割：

- 行き止まりを塞ぐ
- 道を作る
- Playerの停止位置を調整する
- Wallの代替として利用する

### 重要なゲーム性
ランダム生成でWallが配置されると、行き止まりや袋小路が発生する。
Boxを使ってその場所を塞ぐことで、Playerが通れる道を作る戦略を生む。

---

## 2.4 Goal条件

### クリア条件
PlayerがGoalのマスに到達したらステージクリア。

### 今回は採用しない要素
- Boxを特定位置へ運ぶ
- 複数Goal
- 鍵と扉

これらは将来の追加要素として扱う。

---

## 3. ターン処理

## 3.1 1ターンの処理順

1. プレイヤーが重力方向を入力
2. Playerが指定方向へ滑る
3. Boxが指定方向へ滑る
4. Playerの敵接触判定
5. Enemyが移動
6. 次の入力待ち

---

## 3.2 Enemyによる接触ルール

### 重要仕様
EnemyがPlayerに突っ込んでもゲームオーバーにはしない。

ゲームオーバー扱いになるのは、あくまで以下のみ。

```text
Playerが移動した結果、Enemyに接触した場合
```

### 理由
本作は回避アクションではなく、思考型パズルであるため。
Enemyが勝手にPlayerへ突っ込んで失敗になると、プレイヤーに理不尽感が出る。

---

## 4. Enemy仕様

## 4.1 Enemy共通ルール

- Enemyは重力変更によって滑らない
- Enemyは各ターンのPlayer移動後に移動する
- Enemyは追尾しない
- Enemyはあらかじめ設定された方向にのみ移動する
- EnemyがPlayerへ移動してもゲームオーバーにはしない

---

## 4.2 Enemy1

### 移動方向
上下左右のうち、設定された1方向に1マス移動する。

例：
- 上のみ
- 下のみ
- 左のみ
- 右のみ

### 移動失敗時
移動先が以下の場合は移動しない。

- Wall
- Stage外
- Box
- 他Enemy

---

## 4.3 Enemy2

### 移動方向
斜め4方向のうち、設定された1方向に1マス移動する。

例：
- 左上
- 右上
- 左下
- 右下

### 移動失敗時
移動先が以下の場合は移動しない。

- Wall
- Stage外
- Box
- 他Enemy

---

## 5. 失敗・リトライ仕様

## 5.1 失敗条件
Playerが移動した結果、Enemyのいるマスに接触した場合、ステージ失敗。

## 5.2 ゲームオーバー
ゲーム全体のGameOverは基本的になし。

## 5.3 失敗時の選択肢
- リトライ
- リワード広告でステージスキップ
- リワード広告で新しいランダム面を生成
- 諦める / タイトルへ戻る

---

## 6. ステージサイズ

| ステージ帯 | サイズ |
|---|---|
| 1〜10 | 3×3 |
| 11〜30 | 4×4 |
| 31〜50 | 5×5 |
| 51以降 | ランダム |

---

## 7. オブジェクト配置数

## 7.1 Wall最大数

| サイズ | Wall最大数 |
|---|---:|
| 3×3 | 2 |
| 4×4 | 4 |
| 5×5 | 6 |

---

## 7.2 Box数

| サイズ | Box数 |
|---|---:|
| 3×3 | 1〜2 |
| 4×4 | 3〜4 |
| 5×5 | 4〜6 |

---

## 7.3 Enemy数

| サイズ | Enemy数 |
|---|---:|
| 3×3 | 0 |
| 4×4 | 0〜1 |
| 5×5 | 1〜2 |

---

## 8. Wall生成ルール

## 8.1 基本方針
Wallはランダム配置可能。

以下のような配置は許可する。

```text
□ □
```

```text
□ . □
```

つまり、Wall同士の横隣接や1マス空き配置は基本的に許可する。

---

## 8.2 角付近の禁止ルール

ステージ角部分で、Wallが斜め1マスに隣接してPlayerを閉じ込める形は禁止。

禁止例：

```text
. □
□ .
```

この形がステージの角に発生すると、Playerが閉じ込められる可能性があるため禁止する。

### 判定対象
各ステージの4隅について、角マス周辺の2×2を確認する。

禁止パターン例：

左上角：

```text
. □
□ .
```

右上角：

```text
□ .
. □
```

左下角：

```text
□ .
. □
```

右下角：

```text
. □
□ .
```

※実装時は座標系に合わせて判定すること。

---

## 9. ランダム生成仕様

## 9.1 生成対象
ランダム生成では以下を配置する。

- Player
- Goal
- Wall
- Box
- Enemy

---

## 9.2 生成手順

1. ステージサイズを決定
2. 空グリッドを作成
3. Player位置を決定
4. Goal位置を決定
5. Wallを制約条件内で配置
6. Boxをサイズごとの範囲内で配置
7. Enemyをサイズごとの範囲内で配置
8. 到達可能チェックを行う
9. クリア可能なら採用
10. クリア不可なら再生成

---

## 9.3 到達可能チェック

Wall数を制限しても、絶対にクリア可能になるわけではない。
そのため、生成後にPlayerからGoalへ到達可能かを検証する。

### 最低限必要な検証
- PlayerがGoalへ到達できる可能性があるか
- Playerが初期配置で完全に閉じ込められていないか
- Goalが完全に閉じ込められていないか

### 推奨
Solverを用いて、実際の重力移動ルールでGoal到達可能かを確認する。

---

## 9.4 Solver方針

### 目的
ランダム生成されたステージがクリア可能かを判定する。

### 探索方式
BFS（幅優先探索）を推奨。

### 探索対象
- Player位置
- Box位置
- Enemy位置
- 手数

### 入力候補
各状態から以下4方向を試す。

- 上
- 下
- 左
- 右

### 採用条件
Goal到達できる状態が見つかれば、そのステージは採用可能。

---

## 9.5 シード生成

### 概要
同じSeed値を使えば、同じステージを再現できるようにする。

### 用途
- デイリー迷宮
- バグ再現
- SNS共有
- ランキング対象ステージの統一

### 実装例
```csharp
Random.InitState(seed);
```

---

## 10. 星評価

## 10.1 最短手数目安

| サイズ | 最短目安 |
|---|---:|
| 3×3 | 5手 |
| 4×4 | 7手 |
| 5×5 | 10手 |

---

## 10.2 評価条件

| 評価 | 条件 |
|---|---|
| ★★★ | 最短手数以内 |
| ★★ | 最短手数 × 1.5以内 |
| ★ | クリア |

### 例
4×4で最短手数7手の場合：

| 評価 | 条件 |
|---|---|
| ★★★ | 7手以内 |
| ★★ | 10手以内 |
| ★ | 11手以上 |

---

## 11. リワード広告仕様

## 11.1 実装候補

### 優先度高
- ステージスキップ
- 新しいランダム面を生成
- 追加3ステージプレイ

### 後日実装候補
- ヒント表示

---

## 11.2 ヒントについて

ヒントは、ランダム生成されたステージに対してSolverで最適解を計算し、次の1手を表示する必要がある。

実装難度が高いため、Ver1.0では必須にしない。

---

## 11.3 デイリー追加プレイ
1日3ステージまで無料。
リワード広告視聴で追加3ステージをプレイ可能にする。

---

## 12. ランキング仕様

## 12.1 方針
Firebase実装が重い場合は、以下の公式ランキング機能を優先する。

- Apple Game Center
- Google Play Games Services

---

## 12.2 ランキング種別

### 累積ランキング
累積★獲得数で競う。

### デイリーランキング
その日のデイリー迷宮での成績を競う。

候補：

- クリア手数
- クリア時間
- 獲得星数

Ver1.0では累積★獲得数を優先する。

---

## 12.3 ユーザー名
Game Center / Google Play Gamesを利用する場合、基本的には各プラットフォームのユーザー名を利用する。

独自ユーザー名登録・重複チェックはFirebase等を使う場合のみ検討する。

---

## 13. セーブデータ仕様

## 13.1 保存項目

- 現在の到達ステージ
- 各ステージの獲得星数
- 累計星数
- デイリー挑戦回数
- 広告視聴による追加挑戦回数
- 設定情報
- BGM設定
- SE設定

---

## 13.2 保存方式
Ver1.0ではPlayerPrefsで保存する。

将来的にクラウド保存を行う場合は、Game Center / Google Play Gamesのクラウド機能、またはFirebaseを検討する。

---

## 14. シーン構成

## 14.1 TitleScene

### 機能
- ゲーム開始
- ステージ選択
- ランキング表示
- 設定
- デイリーステージ開始

---

## 14.2 GameScene

### 機能
- ステージ表示
- Player表示
- Box表示
- Enemy表示
- Goal表示
- 重力方向ボタン
- 手数表示
- リトライ
- スキップ広告
- 新規生成広告

---

## 14.3 ResultScene または ResultPanel

### 機能
- クリア表示
- 手数表示
- 星評価表示
- 次のステージへ
- リトライ
- タイトルへ戻る

---

## 15. Unityクラス設計案

## 15.1 GameManager
ゲーム全体の状態管理。

担当：

- ターン管理
- クリア判定
- 失敗判定
- ステージ開始・終了
- 手数管理

---

## 15.2 StageGenerator
ステージ生成担当。

担当：

- ランダム生成
- シード生成
- Wall配置
- Box配置
- Enemy配置
- 生成制約チェック
- Solver呼び出し

---

## 15.3 StageData
ステージ情報を保持するデータクラス。

保持項目：

- ステージID
- サイズ
- Player初期位置
- Goal位置
- Wall座標一覧
- Box座標一覧
- Enemy情報一覧
- Seed
- 最短手数

---

## 15.4 PlayerController
Playerの表示・移動担当。

担当：

- グリッド座標管理
- 滑り移動
- 移動アニメーション
- Enemy接触判定

---

## 15.5 BoxController
Boxの表示・移動担当。

担当：

- グリッド座標管理
- 滑り移動
- Wall代替判定

---

## 15.6 EnemyController
Enemyの表示・移動担当。

担当：

- Enemy種類管理
- 移動方向管理
- 1マス移動
- 移動不可判定

---

## 15.7 GridManager
グリッド状態管理。

担当：

- 各マスの占有状態
- 移動可能判定
- 座標変換
- Wall / Box / Enemy / Goal判定

---

## 15.8 SaveManager
セーブ管理。

担当：

- PlayerPrefs保存
- PlayerPrefs読み込み
- 星数保存
- デイリー挑戦回数保存

---

## 15.9 AdManager
広告管理。

担当：

- リワード広告表示
- スキップ処理
- 追加ステージ解放
- 新規生成

---

## 15.10 RankingManager
ランキング管理。

担当：

- Game Center連携
- Google Play Games連携
- 累積星数送信
- ランキング表示

---

## 16. データ構造例

## 16.1 StageData例

```csharp
[System.Serializable]
public class StageData
{
    public int stageId;
    public int width;
    public int height;
    public int seed;
    public Vector2Int playerStart;
    public Vector2Int goalPosition;
    public List<Vector2Int> wallPositions;
    public List<Vector2Int> boxPositions;
    public List<EnemyData> enemies;
    public int targetMoveCount;
}
```

---

## 16.2 EnemyData例

```csharp
[System.Serializable]
public class EnemyData
{
    public EnemyType enemyType;
    public Vector2Int startPosition;
    public Vector2Int moveDirection;
}

public enum EnemyType
{
    Orthogonal,
    Diagonal
}
```

---

## 17. MVP実装順序

## Phase 1：基本移動
- 3×3ステージ
- Player移動
- Wall判定
- Goal判定

## Phase 2：Box実装
- Box配置
- Box移動
- Boxを壁として扱う

## Phase 3：Enemy実装
- Enemy1
- Enemy2
- Player接触判定
- Enemy移動

## Phase 4：ランダム生成
- Wall制約
- Box数制約
- Enemy数制約
- 角の斜めWall禁止
- 到達可能チェック

## Phase 5：星評価
- 手数管理
- 星計算
- Result表示

## Phase 6：広告
- スキップ
- 新規生成
- 追加3ステージ

## Phase 7：ランキング
- Game Center
- Google Play Games

---

## 18. Codexへの依頼例

Unityプロジェクト作成後、Codexには以下のように依頼する。

```text
SPEC.md V2を読み込んでください。
まずPhase 1として、3×3グリッド上でPlayerが上下左右に滑り、Wallに当たるまで移動し、Goalに到達したらクリア判定を出す最小実装を作ってください。
PrefabやUIボタンは私がUnity上で配置します。
必要なC#スクリプト、Inspector設定、Hierarchy構成案を提示してください。
```

---

## 19. 今後の追加要素候補

- Boxを指定位置へ運ぶステージ
- 鍵と扉
- ワープ
- 一方通行床
- 壊れる床
- 移動床
- 複数Goal
- 特殊Box
- ヒント機能
- デイリーチャレンジ強化
- オンラインランキング強化

---

## 20. 注意事項

### 20.1 ランダム生成について
Wall数制限だけではクリア不能ステージを完全には防げない。
必ず生成後に到達可能チェックを行うこと。

### 20.2 Enemyについて
EnemyがPlayerへ突っ込んでも失敗にはしない。
失敗になるのはPlayerが移動してEnemyに接触した場合のみ。

### 20.3 ランキングについて
Firebaseを使わない場合、Game Center / Google Play Gamesを利用する。
独自ユーザー名や重複チェックは原則不要。

---

以上。
